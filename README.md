# CHIP-8 Emulator

This project is a CHIP-8 emulator written in Rust. It lets you run classic 8-bit CHIP-8 games on a modern computer. The emulator implements the full CHIP-8 instruction set, handles graphics, keyboard input, and timers, and displays using Raylib.
